{% include 'default/_form.html.twig' with {
        page_title: 'title.todoitem_delete'|trans({'%id%': todoitem.id|default('')}),
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'todoitem_index',
    } %}